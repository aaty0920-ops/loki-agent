#!/usr/bin/env python3
"""
Loki Agent CLI launcher.

This wrapper should behave like the installed `loki` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from loki_cli.main import main
    main()
